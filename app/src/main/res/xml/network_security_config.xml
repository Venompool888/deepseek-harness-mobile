<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Runtime validation limits HTTP endpoints to private LAN hosts. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
